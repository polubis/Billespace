method: GET
description: Manages log in procedure.
params:
  redirectionUrl: url()
errors:
  url():
    message: Redirection url have invalid format.
scenarios:
  invalid params:
    error: url()
    code: 400
  user not logged in:
    actions:
      - Redirects to Microsoft auth provider.
    code: 300
  user already logged in:
    code: 200
